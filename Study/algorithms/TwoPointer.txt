📌 투포인터(Two Pointers) 초간단 정리

✔ 개념

정렬된 배열에서 두 개의 인덱스(i, j) 를 양쪽에 두고
조건(합/부분합/범위 등)을 만족할 때까지 이동시키는 방식.
→ 시간복잡도 O(n).

✔ 사용 조건

배열이 정렬되어 있거나 정렬이 필요할 때
두 수의 합/차, 구간 합, 부분수열 길이 등을 찾을 때
투포인터 = 메모리 포인터 X → 그냥 인덱스 두 개

✔ 기본 알고리즘 (두 수의 합 예시)

arr.sort()
i = 0
j = len(arr) - 1

while i < j:
    s = arr[i] + arr[j]

    if s < target: 
        i += 1        # 합 키우기
    elif s > target:
        j -= 1        # 합 줄이기
    else:
        count += 1
        i += 1
        j -= 1

✔ 핵심 이동 규칙

합이 작으면 왼쪽 인덱스 증가
합이 크면 오른쪽 인덱스 감소
합이 같으면 정답 + 둘 다 이동

✔ 장점

O(n)에 쌍/구간 탐색 가능
이중 for문(O(n²))을 대체함
정렬 기반 탐색에 매우 강력함
